{
  "name": "alparslan",
  "version": "0.3.0",
  "description": "Guvenli tarayici eklentisi - Phishing ve dolandiriciliga karsi kalkan",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite build --watch --mode development",
    "build": "tsc --noEmit && vite build && vite build --mode content",
    "build:firefox": "tsc --noEmit && vite build --mode firefox && vite build --mode firefox-bg && vite build --mode firefox-content",
    "build:safari": "tsc --noEmit && vite build --mode safari && vite build --mode safari-content",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/",
    "format": "prettier --write \"src/**/*.{ts,tsx,css,json}\"",
    "package": "npm run build && rm -f dist.zip && cd dist && zip -r ../dist.zip . && cd ..",
    "package:firefox": "npm run build:firefox && rm -f dist-firefox.zip && cd dist-firefox && rm -f manifest.firefox.json && zip -r ../dist-firefox.zip . && cd ..",
    "package:safari": "npm run build:safari && rm -f dist-safari.zip && cd dist-safari && rm -f manifest.safari.json && zip -r ../dist-safari.zip . && cd ..",
    "test:e2e": "npx playwright test --config e2e/playwright.config.ts",
    "test:e2e:headed": "npx playwright test --config e2e/playwright.config.ts --headed",
    "test:e2e:debug": "npx playwright test --config e2e/playwright.config.ts --debug",
    "test:e2e:report": "npx playwright show-report"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "tldts": "^7.4.2"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@types/chrome": "^0.0.268",
    "@types/react": "^18.3.3",
    "@types/react-dom": "^18.3.0",
    "@typescript-eslint/eslint-plugin": "^7.16.0",
    "@typescript-eslint/parser": "^7.16.0",
    "@vitejs/plugin-react": "^4.3.1",
    "eslint": "^8.57.0",
    "eslint-config-prettier": "^9.1.0",
    "fake-indexeddb": "^6.2.5",
    "happy-dom": "^20.8.9",
    "jsdom": "^29.0.1",
    "prettier": "^3.3.3",
    "typescript": "^5.5.3",
    "vite": "^5.3.4",
    "vitest": "^2.0.3"
  }
}
