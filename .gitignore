node_modules/
dist/
dist-firefox/
dist-safari/
Alparslan/
*.zip
*.log
.DS_Store
.env
.env.local
# Claude Code: shared team config (CLAUDE.md, .claude/settings.json, .claude/commands/)
# is tracked; only per-developer local overrides are ignored.
.claude/settings.local.json
.planning/
playwright-report
test-results
docs
.idea
.wolf/

# Bundled build output must live in dist/, never in repo root.
# (list.js / whitelist.js are hand-written source pages copied verbatim
#  into dist/ by vite.config.ts, so they are intentionally NOT ignored.)
/background.js
/content.js
/popup.js
/options.js
/chunks/
